import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.shusseuo",
  appName: "出世魚",
  webDir: "out",
  ios: {
    contentInset: "always",
  },
};

export default config;
